# AI Co-Scientist API + Desktop Dependencies
# Install with: pip install -r requirements-api.txt

# FastAPI Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Pydantic for data validation (already in main requirements)
pydantic>=2.0
pydantic-settings>=2.0

# Async HTTP client
httpx>=0.26.0
aiohttp>=3.9.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Structured logging
structlog>=23.2.0

# Phase A (desktop packaging): SQLite, secret storage, encryption fallback
aiosqlite>=0.20.0
keyring>=24.0.0
cryptography>=42.0.0

# Email validation (Pydantic EmailStr field)
email-validator>=2.0.0

# LLM clients (imported at module load by src/llm/*_client.py).
# Required so the Phase 5 tests AND the bundled desktop sidecar can load.
langchain>=0.3
langchain-core>=0.3
langchain-openai>=0.2       # DeepSeek client uses ChatOpenAI under the hood
langchain-google-genai>=2.0 # Gemini client
langchain-anthropic>=0.2    # Claude client
anthropic>=0.40

# Agent framework + observability
langgraph>=0.2
langsmith>=0.1.0

# Misc runtime deps
tenacity>=8.0
python-dotenv>=1.0
aiofiles>=23.0

# Note: heavy ML/literature deps (chromadb, faiss-cpu, sentence-transformers,
# pymupdf, networkx, scikit-learn) remain in 03_architecture/environment.yml.
# Phase 5 CI tests don't exercise those paths.
